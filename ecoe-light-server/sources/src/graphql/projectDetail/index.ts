import projectDetailTypedef from './schema';
import projectDetailResolver from './resolve';

export = {
    projectDetailTypedef: projectDetailTypedef,
    projectDetailResolver: projectDetailResolver
}